<%- contentFor('body') %>
  <h2>Editar Registro de Professor</h2>
  <% data.forEach(function(details) { %>
    <form action="/teacher/edit/<%= details._id %>" method="POST">
      <input type="text" value="<%= details.name %>" name="name">
      <input type="text" value="<%= details.surname %>" name="surname">
      <br />
      <button><a href="/teacher/show">Todos</a></button>
      <button type="submit">Editar</button>
    </form>
    <% }) %>